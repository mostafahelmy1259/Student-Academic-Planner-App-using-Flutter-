# Firebase Setup Required

Firebase configuration files are not included in this public repository.

Before running the app, create your own Firebase project and run:

```powershell
firebase login
flutterfire configure
flutter pub get
flutter run
